version: "3.3"

services:
  database:
    container_name: smile-det-mongo
    image: mongo:latest

    volumes:
      - /mongodb/data:/data/db

    ports:
      - 27017:27017
      - 27019:27019

  rabbitmq:
    image: rabbitmq:3.8.3
    ports:
      - "5672:5672"

volumes:
  data:
